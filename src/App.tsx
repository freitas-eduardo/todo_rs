
import { Task } from './components/task'
import { useTodo } from './hooks'


function App() {
  const { todo, removeTask, toggleTask } = useTodo()

  return (
    <main>
      <header>

      </header>
      <section >

      </section>

    </main>
  )
}

export default App
